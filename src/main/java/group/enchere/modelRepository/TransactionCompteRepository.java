package group.enchere.modelRepository;

import group.enchere.model.TransactionCompte;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TransactionCompteRepository extends JpaRepository<TransactionCompte,Integer> {
}
